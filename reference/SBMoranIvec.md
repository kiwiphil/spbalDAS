# Inverse-distance spread used by MATLAB SBMoranIvec.

`2 * sum(W[A, C])` for each candidate in `A`. Kept for tests and for
comparing against the C++ cost fill.

## Usage

``` r
SBMoranIvec(C, A, W)
```
